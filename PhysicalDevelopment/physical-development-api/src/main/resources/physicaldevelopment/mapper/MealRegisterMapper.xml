<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="physicaldevelopment.datasource.mapper.MealRegisterMapper">

	<select id="insertMeal" parameterType="physicaldevelopment.model.meal.Meal">
		INSERT INTO nutrition.meal (
			meal_id,
			daily_nutrient_amount_id,
			order_of_meals,
			meal_name,
			input_type,
			created_at,
			updated_at
		) VALUES (
			#{mealId.mealId},
			#{一日の栄養素量ID},
			#{orderOfMeals.orderOfMeals},
			#{mealName.mealName},
			#{inputType.name},
			now(),
			now()
		)
	</select>

	<select id="insertMealManual" parameterType="physicaldevelopment.model.meal.Meal">
		INSERT INTO nutrition.meal (
			manual_entry_of_nutrients_id,
			meal_id,
			nutrition_type,
			created_at,
			updated_at
		) VALUES (
			#{mealId.mealId},
			#{一日の栄養素量ID},
			#{orderOfMeals.orderOfMeals},
			#{mealName.mealName},
			#{inputType.name},
			now(),
			now()
		)
	</select>

	<select id="selectNextAccountId" parameterType="physicaldevelopment.model.account.Account" resultType="int">
		SELECT nextval('account.account_account_id_SEQ')
	</select>


</mapper>